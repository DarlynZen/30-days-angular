## Descripción

Este proyecto es una aplicación simple de contador desarrollada en Angular, que permite incrementar, decrementar y reiniciar un valor numérico mediante una interfaz interactiva.

## Crear un componente

Para generar un nuevo componente en Angular:

```bash
ng generate component nombre-del-componente
